# Анализ влияния изменений на функциональность

**Дата:** 2024-12-19

## 📊 Общая оценка

| Пункт | Влияние на функциональность | Риск | Требует миграции данных |
|-------|----------------------------|------|------------------------|
| 1. Формат size_code | ⚠️ **ВЫСОКИЙ** | 🔴 Критично | ✅ Да |
| 2. Нормализация color_id | ✅ **НЕТ** | 🟢 Низкий | ❌ Нет |
| 3. Удаление двойной валидации | ⚠️ **СРЕДНИЙ** | 🟡 Средний | ❌ Нет |
| 4. Константы детских размеров | ✅ **НЕТ** | 🟢 Низкий | ❌ Нет |
| 5. Функция нормализации размера | ✅ **НЕТ** | 🟢 Низкий | ❌ Нет |
| 6. Исправление расчета остатков | ⚠️ **СРЕДНИЙ** | 🟡 Средний | ❌ Нет |
| 7. receipt_id/realization_id | ✅ **НЕТ** | 🟢 Низкий | ❌ Нет |

---

## 🔴 Пункт 1: Унифицировать формат size_code

### ⚠️ **ВЛИЯНИЕ: ВЫСОКИЙ РИСК**

### Текущая ситуация:

**В базе данных уже есть данные:**
- `receipt_items.size_code` = `"92"` (обрезанный формат)
- `realization_items.size_code` = `"92"` (обрезанный формат)
- `sizes.code` = `"92 - 2 года"` (полный формат)

**Запросы используют точное сравнение:**
```typescript
// pages/api/realization/create.ts:75
.eq('size_code', sizeVariant)  // Ищет "92"

// lib/stockService.ts:48
.eq('size_code', sizeCode)  // Ищет "92"
```

### ❌ Что может сломаться:

**Если изменим на полный формат:**

1. **Существующие данные не найдутся:**
   ```typescript
   // В БД: size_code = "92"
   // Запрос: .eq('size_code', "92 - 2 года")
   // Результат: НЕ НАЙДЕТ! ❌
   ```

2. **Расчет остатков сломается:**
   ```typescript
   // receipt_items: size_code = "92"
   // realization_items: size_code = "92"
   // Ключ: `${productId}_92_${colorId}` ✅ Работает
   
   // После изменения:
   // receipt_items: size_code = "92 - 2 года"
   // realization_items: size_code = "92"
   // Ключ: `${productId}_92 - 2 года_${colorId}` vs `${productId}_92_${colorId}`
   // Результат: НЕ СОВПАДЕТ! ❌
   ```

3. **Валидация детских размеров:**
   ```typescript
   // Сейчас: сравнивает "92" с ['92', '98', ...] ✅
   // После: сравнивает "92 - 2 года" с ['92', '98', ...] ❌
   ```

### ✅ Решение (безопасный вариант):

**Вариант А: Оставить числовой формат везде (РЕКОМЕНДУЕТСЯ)**

1. **Обновить таблицу `sizes`:**
   ```sql
   -- Миграция: обрезать размеры до числовой части
   UPDATE sizes 
   SET code = SPLIT_PART(code, ' ', 1) 
   WHERE code LIKE '% - %';
   ```

2. **Обновить API `sizes/by-category.ts`:**
   ```typescript
   // Возвращать только числовую часть
   '92', '98', '104', ...  // вместо "92 - 2 года"
   ```

3. **Убрать обрезание при сохранении:**
   ```typescript
   // БЫЛО:
   size_code: item.size_code.split(' ')[0]
   
   // ДОЛЖНО БЫТЬ:
   size_code: item.size_code  // Уже числовой формат
   ```

**Преимущества:**
- ✅ Не ломает существующие данные
- ✅ Проще сравнения
- ✅ Меньше места в БД

**Недостатки:**
- ⚠️ Теряется информация о возрасте (но она не используется в логике)

**Вариант Б: Перейти на полный формат (ТРЕБУЕТ МИГРАЦИИ)**

1. **Миграция данных:**
   ```sql
   -- Обновить receipt_items и realization_items
   UPDATE receipt_items ri
   SET size_code = s.code
   FROM sizes s
   WHERE SPLIT_PART(s.code, ' ', 1) = ri.size_code
     AND s.code LIKE '% - %';
   
   -- То же для realization_items
   ```

2. **Обновить все запросы:**
   - Использовать `extractSizeNumber()` для сравнения
   - Сохранять полный формат

**Преимущества:**
- ✅ Сохраняется информация о возрасте
- ✅ Соответствует таблице `sizes`

**Недостатки:**
- ⚠️ Требует миграции данных
- ⚠️ Более сложные запросы
- ⚠️ Риск потери данных при миграции

### 🎯 Рекомендация:

**Выбрать Вариант А** (числовой формат везде) - безопаснее и проще.

---

## ✅ Пункт 2: Унифицировать обработку color_id

### ✅ **ВЛИЯНИЕ: НЕТ (рефакторинг)**

### Текущая ситуация:

Разные способы обработки, но **логика эквивалентна**:
- `color_id = 0` → `null` (везде)
- `color_id = null` → `null` (везде)
- `color_id = число` → число (везде)

### ✅ Что изменится:

**Только структура кода, не поведение:**

```typescript
// БЫЛО:
if (item.color_id !== null && item.color_id !== undefined && item.color_id !== 0) {
  const colorId = parseInt(item.color_id);
  if (isNaN(colorId) || colorId <= 0) {
    return res.status(400).json({ error: 'Некорректный ID цвета' });
  }
}
color_id: item.color_id || null

// ДОЛЖНО БЫТЬ:
const colorId = normalizeColorId(item.color_id);
if (colorId === null && colorRequired) {
  return res.status(400).json({ error: 'Цвет обязателен' });
}
color_id: colorId
```

### ✅ Проверка эквивалентности:

| Входное значение | Старая логика | Новая логика | Результат |
|-----------------|---------------|--------------|-----------|
| `null` | `null` | `null` | ✅ Одинаково |
| `undefined` | `null` | `null` | ✅ Одинаково |
| `0` | `null` | `null` | ✅ Одинаково |
| `"0"` | `null` | `null` | ✅ Одинаково |
| `5` | `5` | `5` | ✅ Одинаково |
| `"5"` | `5` | `5` | ✅ Одинаково |
| `-1` | Проверка на `<= 0` → ошибка | `null` | ⚠️ Разница |

### ⚠️ Потенциальная проблема:

**Отрицательные числа:**
- Старая логика: `color_id = -1` → ошибка валидации
- Новая логика: `color_id = -1` → `null`

**Решение:** Добавить валидацию, если нужна:
```typescript
const colorId = normalizeColorId(item.color_id);
if (colorId === null && colorRequired) {
  return res.status(400).json({ error: 'Цвет обязателен' });
}
// Если colorId === null и colorRequired === false, то OK
```

### 🎯 Итог:

**Безопасно:** Логика эквивалентна, поведение не изменится.

---

## ⚠️ Пункт 3: Удалить двойную валидацию остатков

### ⚠️ **ВЛИЯНИЕ: СРЕДНИЙ (исправление бага)**

### Текущая ситуация:

**Проверка 1 (неправильная):**
- Ищет только в `receipt_items`
- Не учитывает `realization_items`
- Может показать товар как доступный, хотя он уже реализован

**Проверка 2 (правильная):**
- Использует `validateStockForItems`
- Учитывает `receipt_items - realization_items`

### ❌ Что может измениться:

**Сценарий 1: Товар уже реализован полностью**

```typescript
// Поступления: 10 шт
// Реализации: 10 шт
// Остаток: 0 шт

// Сейчас:
// Проверка 1: находит 10 шт в receipt_items → ✅ Пропускает
// Проверка 2: остаток 0 → ❌ Блокирует
// Результат: Блокируется (правильно)

// После удаления проверки 1:
// Проверка 2: остаток 0 → ❌ Блокирует
// Результат: Блокируется (правильно) ✅
```

**Сценарий 2: Товар частично реализован**

```typescript
// Поступления: 10 шт
// Реализации: 5 шт
// Остаток: 5 шт
// Запрос: 7 шт

// Сейчас:
// Проверка 1: находит 10 шт → ✅ Пропускает (НЕПРАВИЛЬНО!)
// Проверка 2: остаток 5 шт, запрос 7 шт → ❌ Блокирует
// Результат: Блокируется (правильно)

// После удаления проверки 1:
// Проверка 2: остаток 5 шт, запрос 7 шт → ❌ Блокирует
// Результат: Блокируется (правильно) ✅
```

**Сценарий 3: Товар не реализован**

```typescript
// Поступления: 10 шт
// Реализации: 0 шт
// Остаток: 10 шт
// Запрос: 5 шт

// Сейчас:
// Проверка 1: находит 10 шт → ✅ Пропускает
// Проверка 2: остаток 10 шт, запрос 5 шт → ✅ Пропускает
// Результат: Разрешается (правильно)

// После удаления проверки 1:
// Проверка 2: остаток 10 шт, запрос 5 шт → ✅ Пропускает
// Результат: Разрешается (правильно) ✅
```

### ✅ Итог:

**Безопасно:** Поведение не изменится, так как проверка 1 неправильная и не влияет на результат (всегда блокируется проверкой 2, если нужно).

**Преимущество:** Упрощение кода, удаление неправильной логики.

---

## ✅ Пункт 4: Вынести константы детских размеров

### ✅ **ВЛИЯНИЕ: НЕТ (рефакторинг)**

### Текущая ситуация:

Одинаковый список в 3 местах:
```typescript
['92', '98', '104', '110', '116', '122', '134', '140', '146', '152', '158', '164']
```

### ✅ Что изменится:

**Только структура кода:**

```typescript
// БЫЛО:
const childrenSizes = ['92', '98', '104', ...];

// ДОЛЖНО БЫТЬ:
import { CHILDREN_SIZES } from '../../../lib/constants';
// CHILDREN_SIZES = ['92', '98', '104', ...]
```

### ✅ Проверка:

Значения **идентичны**, поведение **не изменится**.

### 🎯 Итог:

**Безопасно:** Только рефакторинг, функциональность не меняется.

---

## ✅ Пункт 5: Функция нормализации размера

### ✅ **ВЛИЯНИЕ: НЕТ (рефакторинг)**

### Текущая ситуация:

Разные способы извлечения, но **результат одинаковый**:
- `split(' ')[0]` → `"92"`
- `split(' ')[0].trim()` → `"92"`

### ✅ Что изменится:

**Только структура кода:**

```typescript
// БЫЛО:
const sizeNumber = item.size_code.split(' ')[0];
const sizeNumber = size.split(' ')[0].trim();

// ДОЛЖНО БЫТЬ:
import { extractSizeNumber } from '../../../lib/utils/normalize';
const sizeNumber = extractSizeNumber(item.size_code);
```

### ✅ Проверка:

Функция делает то же самое, что и текущий код.

### 🎯 Итог:

**Безопасно:** Только рефакторинг, поведение не меняется.

---

## ⚠️ Пункт 6: Исправить расчет остатков

### ⚠️ **ВЛИЯНИЕ: СРЕДНИЙ (исправление бага)**

### Текущая ситуация:

В `realization/create.ts` (строки 52-105) расчет **неправильный**:
- Учитывает только `receipt_items`
- Не учитывает `realization_items`

Но эта проверка **не влияет на результат**, так как есть правильная проверка 2.

### ✅ Что изменится:

**Удаление неправильной проверки** (см. пункт 3).

### 🎯 Итог:

**Безопасно:** Поведение не изменится, так как неправильная проверка не влияет на результат.

---

## ✅ Пункт 7: Устанавливать receipt_id/realization_id

### ✅ **ВЛИЯНИЕ: НЕТ (улучшение структуры)**

### Текущая ситуация:

Внешние ключи не устанавливаются при создании, связь по времени.

### ✅ Что изменится:

**Только структура данных:**

```typescript
// БЫЛО:
{
  product_id: 1,
  qty: 10,
  size_code: "92",
  color_id: 5
  // НЕТ receipt_id
}

// ДОЛЖНО БЫТЬ:
{
  product_id: 1,
  qty: 10,
  size_code: "92",
  color_id: 5,
  receipt_id: 123  // ✅ Добавляется
}
```

### ✅ Влияние на функциональность:

**Нет влияния:**
- Существующий код работает с time-based linking
- Новый код будет работать с внешними ключами
- Оба способа работают параллельно

**Преимущества:**
- ✅ Более надежная связь
- ✅ Не нужен скрипт `fill-foreign-keys.js`
- ✅ Лучшая производительность запросов

### 🎯 Итог:

**Безопасно:** Только улучшение, функциональность не меняется.

---

## 📊 Итоговая таблица влияния

| Пункт | Влияние | Риск | Требует миграции | Рекомендация |
|-------|---------|------|------------------|--------------|
| 1. Формат size_code | ⚠️ Высокий | 🔴 Критично | ✅ Да | Выбрать числовой формат |
| 2. color_id | ✅ Нет | 🟢 Низкий | ❌ Нет | ✅ Безопасно |
| 3. Двойная валидация | ⚠️ Средний | 🟡 Средний | ❌ Нет | ✅ Безопасно (исправление) |
| 4. Константы | ✅ Нет | 🟢 Низкий | ❌ Нет | ✅ Безопасно |
| 5. Нормализация размера | ✅ Нет | 🟢 Низкий | ❌ Нет | ✅ Безопасно |
| 6. Расчет остатков | ⚠️ Средний | 🟡 Средний | ❌ Нет | ✅ Безопасно (исправление) |
| 7. Внешние ключи | ✅ Нет | 🟢 Низкий | ❌ Нет | ✅ Безопасно |

---

## 🎯 План безопасного внедрения

### Этап 1: Безопасные изменения (можно делать сразу)

1. ✅ Пункт 2: Нормализация `color_id`
2. ✅ Пункт 4: Константы детских размеров
3. ✅ Пункт 5: Функция нормализации размера
4. ✅ Пункт 7: Внешние ключи

### Этап 2: Исправления багов (требует тестирования)

5. ⚠️ Пункт 3: Удаление двойной валидации
6. ⚠️ Пункт 6: Исправление расчета остатков

**Тестирование:**
- Проверить создание реализации с разными остатками
- Проверить блокировку при недостаточном остатке
- Проверить разрешение при достаточном остатке

### Этап 3: Критическое изменение (требует миграции)

7. 🔴 Пункт 1: Формат `size_code`

**План миграции:**
1. Создать резервную копию БД
2. Выполнить миграцию данных (если выбран полный формат)
3. Обновить код
4. Протестировать все запросы
5. Откатить, если проблемы

---

## ⚠️ Критические риски

### Риск 1: Формат size_code

**Если изменим формат без миграции:**
- ❌ Существующие данные не найдутся
- ❌ Расчет остатков сломается
- ❌ Реализации не будут работать

**Решение:** Обязательно выполнить миграцию данных.

### Риск 2: Удаление валидации

**Если удалим неправильную проверку:**
- ✅ Поведение не изменится (проверка 2 правильная)
- ✅ Но нужно протестировать все сценарии

**Решение:** Тщательное тестирование перед деплоем.

---

## ✅ Выводы

1. **Большинство изменений безопасны** (пункты 2, 4, 5, 7) - только рефакторинг
2. **Исправления багов** (пункты 3, 6) - улучшают правильность, но требуют тестирования
3. **Критическое изменение** (пункт 1) - требует миграции данных и тщательного планирования

**Рекомендация:** Начать с безопасных изменений (Этап 1), затем исправления багов (Этап 2), и только потом критическое изменение (Этап 3).

