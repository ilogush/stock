# Логика работы системы - Описание страниц и их взаимодействия

## 📋 Общий принцип работы системы

### Бизнес-процесс:
1. **Создание товара** → 2. **Поступление** → 3. **Склад** → 4. **Реализация** → 5. **Остаток**

### Основная формула:
```
Остаток на складе = Поступления - Реализации
Stock = receipt_items - realization_items
```

### Правила:
- Склад показывает только товары с остатком > 0
- Все товары обрабатываются одинаково независимо от бренда/категории
- Остаток рассчитывается: `Math.max(0, receipt_qty - realization_qty)`
- Товары группируются по: `product_id + size_code + color_id`

---

## 📄 Описание страниц

### 1. Товары (Products) - `/products`

**Назначение:** Управление каталогом товаров

**Файлы:**
- `pages/products/index.tsx` - список товаров
- `pages/products/new.tsx` - создание товара
- `pages/products/[id].tsx` - просмотр/редактирование товара
- `pages/api/products/index.ts` - API получения списка
- `pages/api/products/create.ts` - API создания товара

**Взаимодействие с данными:**

**Товары:**
- Отображение: список всех товаров с фильтрацией по категории и поиском
- Фильтрация: по `category_id`, `brand_id`, поиск по артикулу/названию
- Поля товара: `id`, `name`, `article`, `brand_id`, `category_id`, `color_id`, `price`, `images`
- Связи: `brand:brands(name)`, `category:categories(name)`, `color:colors(name)`

**Цвета:**
- Отображаются через `color_id` → `colorName` из API
- Показываются как название цвета из таблицы `colors`

**Количество:**
- На странице товаров количество не отображается (это каталог)
- Количество есть только на складе

**Правила:**
- Товары можно создавать, редактировать, удалять
- При создании товара без изображений он автоматически скрывается (`is_visible = false`)
- Поиск работает по артикулу, названию, бренду, цвету
- Пагинация: по умолчанию 20 товаров на странице

---

### 2. Поступления (Receipts) - `/receipts`

**Назначение:** Регистрация поступления товаров на склад

**Файлы:**
- `pages/receipts/index.tsx` - список поступлений
- `pages/receipts/new.tsx` - создание поступления
- `pages/receipts/[id].tsx` - просмотр поступления
- `pages/api/receipts/index.ts` - API получения списка
- `pages/api/receipts/create.ts` - API создания поступления
- `pages/api/receipts/[id].ts` - API получения одного поступления

**Взаимодействие с данными:**

**Товары:**
- Выбор товара: по артикулу с автодополнением
- Поиск товара: из таблицы `products` по артикулу
- Валидация: товар должен существовать в системе

**Цвета:**
- Фильтрация: показываются только цвета выбранного товара (`productColors`)
- Источник: из таблицы `products` по `product_id` → `color_id` → `colors`
- Обязательное поле: цвет должен быть выбран

**Размеры:**
- Источник: из таблицы `sizes` с фильтрацией по категории товара
- Сортировка: 
  - Детские (92-164): по числовому значению
  - Взрослые (XS-XXXL): по стандартному порядку
- Обязательное поле: размер должен быть выбран

**Количество:**
- Поле: `quantity` (количество единиц)
- Валидация: минимум 1, только положительные числа
- Сохранение: в таблицу `receipt_items` как `qty`

**Правила:**
- Создание поступления: `receipts` + `receipt_items`
- Связь: `receipt_items` связаны с `receipts` через `receipt_id` (или по времени)
- Обязательные поля: `transferrer_id` (принято от), товары с размером, цветом, количеством
- Валидация детских размеров: для категории 3 (детское) только размеры 92-164

**Структура данных:**
```typescript
receipt_items {
  product_id: number,
  size_code: string,
  color_id: number,
  qty: number,
  receipt_id?: number, // если есть внешний ключ
  created_at: timestamp
}
```

---

### 3. Склад (Stock) - `/stock`

**Назначение:** Отображение остатков товаров на складе

**Файлы:**
- `pages/stock/index.tsx` - отображение склада
- `pages/api/stock/index.ts` - API получения остатков
- `pages/api/stock/stock-details.ts` - API для формы реализации

**Взаимодействие с данными:**

**Товары:**
- Источник: агрегация из `receipt_items` минус `realization_items`
- Группировка: по артикулу (`article`), затем по цветам, затем по размерам
- Фильтрация: по `category_id`, `brand_id`, поиск по артикулу/названию/бренду/цвету
- Отображение: только товары с остатком > 0

**Цвета:**
- Отображение: каждый цвет как отдельная строка в таблице
- Источник: `color_id` из `receipt_items` → название из таблицы `colors`
- Группировка: все цвета одного артикула отображаются отдельными строками

**Размеры:**
- Отображение: размеры как колонки в таблице
- Фильтрация:
  - Детская категория (3): только размеры 92-164
  - Взрослые категории: исключаются детские размеры
- Сортировка:
  - Детские: 92, 98, 104, 110, 116, 122, 134, 140, 146, 152, 158, 164
  - Взрослые: XS, S, M, L, XL, XXL, XXXL
- Показ: для детской категории показываются все размеры, даже если остаток = 0

**Количество:**
- Расчет: `receipt_items.qty - realization_items.qty`
- Формула: `Math.max(0, receipt_qty - realization_qty)`
- Отображение: количество в ячейке на пересечении цвета и размера
- Итоги: сумма по цвету, сумма по артикулу

**Правила:**
- Остаток = поступления - реализации
- Показываются только товары с остатком > 0
- Группировка: артикул → цвета → размеры
- Пагинация: по умолчанию 20 строк (цветов) на странице
- Поиск: по артикулу, названию, бренду, цвету

**Структура ответа API:**
```typescript
{
  items: [{
    article: string,
    name: string,
    brandName: string,
    colors: [{
      colorId: number,
      colorName: string,
      sizes: number[], // массив количеств по размерам
      total: number
    }],
    total: number,
    images: string[]
  }],
  sizes: string[], // список размеров для заголовков
  pagination: {...}
}
```

---

### 4. Реализация (Realization) - `/realization`

**Назначение:** Регистрация отгрузки товаров со склада

**Файлы:**
- `pages/realization/index.tsx` - список реализаций
- `pages/realization/new.tsx` - создание реализации
- `pages/realization/[id].tsx` - просмотр реализации
- `pages/api/realization/index.ts` - API получения списка
- `pages/api/realization/create.ts` - API создания реализации
- `pages/api/stock/stock-details.ts` - API получения доступных товаров

**Взаимодействие с данными:**

**Товары:**
- Источник: из API `/api/stock/stock-details` (только товары с остатком > 0)
- Выбор: по артикулу с автодополнением
- Валидация: товар должен быть на складе с остатком > 0
- Показ цветов: все цвета артикула, независимо от выбранного размера

**Цвета:**
- Источник: из `stock-details` API (все цвета артикула)
- Фильтрация: показываются все цвета, у которых есть остаток на складе
- Выбор: обязательное поле
- Отображение: название цвета из таблицы `colors`

**Размеры:**
- Источник: из `stock-details` API для выбранного артикула и цвета
- Фильтрация: только размеры с остатком > 0 для выбранного цвета
- Выбор: обязательное поле
- Валидация: размер должен иметь остаток на складе

**Количество:**
- Источник: остаток на складе для выбранного товара/цвета/размера
- Валидация: 
  - Нельзя реализовать больше, чем есть на складе
  - Минимум 1 единица
- Сохранение: в таблицу `realization_items` как `qty`

**Правила:**
- Создание реализации: `realization` + `realization_items`
- Связь: `realization_items` связаны с `realization` через `realization_id` (или по времени)
- Обязательные поля: `recipient_id` (получатель), товары с размером, цветом, количеством
- Валидация остатка: проверка через `validateStockForItems` перед созданием
- Валидация детских размеров: для категории 3 (детское) только размеры 92-164

**Структура данных:**
```typescript
realization_items {
  product_id: number,
  size_code: string,
  color_id: number,
  qty: number,
  realization_id?: number, // если есть внешний ключ
  created_at: timestamp
}
```

**Логика выбора товара:**
1. Пользователь вводит артикул
2. Показываются все доступные артикулы с остатком > 0
3. При выборе артикула показываются все его цвета
4. При выборе цвета показываются все размеры с остатком > 0 для этого цвета
5. При выборе размера показывается доступное количество

---

### 5. Цвета (Colors) - `/colors`

**Назначение:** Управление справочником цветов

**Файлы:**
- `pages/colors/index.tsx` - список цветов
- `pages/colors/new.tsx` - создание цвета
- `pages/api/colors/index.ts` - API работы с цветами

**Взаимодействие с данными:**

**Цвета:**
- Отображение: список всех цветов с фильтрацией по табам
- Поля: `id`, `name`, `hex_value`
- Создание: только уникальные цвета (проверка по названию)
- Использование: в товарах (`products.color_id`), поступлениях, реализациях

**Правила:**
- Цвета используются в товарах, поступлениях, реализациях
- Цвет может быть `null` (товар без цвета)
- Название цвета используется для поиска и отображения

---

### 6. Бренды (Brands) - `/brands`

**Назначение:** Управление справочником брендов

**Взаимодействие с данными:**

**Бренды:**
- Связь: `products.brand_id` → `brands.id`
- Использование: фильтрация товаров, отображение на складе
- Поля: `id`, `name`

---

### 7. Категории (Categories) - `/categories`

**Назначение:** Управление справочником категорий

**Взаимодействие с данными:**

**Категории:**
- Связь: `products.category_id` → `categories.id`
- Использование: 
  - Фильтрация товаров и склада
  - Определение доступных размеров
  - Сортировка детских размеров
- Поля: `id`, `name`
- Специальная категория: ID 3 = "Детское" (детские размеры 92-164)

---

## 🔄 Взаимодействие между страницами

### Поток данных:

```
1. Товары (Products)
   ↓ создание товара
   
2. Поступления (Receipts)
   ↓ выбор товара из Products
   ↓ добавление receipt_items
   
3. Склад (Stock)
   ↓ расчет: receipt_items - realization_items
   ↓ отображение остатков
   
4. Реализация (Realization)
   ↓ выбор товара из Stock (только с остатком > 0)
   ↓ добавление realization_items
   
5. Склад (Stock) - обновление
   ↓ пересчет остатков
   ↓ если остаток = 0, товар скрывается
```

### Связи данных:

**Товары ↔ Поступления:**
- Поступления используют товары из `products`
- Валидация: товар должен существовать

**Поступления ↔ Склад:**
- Склад агрегирует данные из `receipt_items`
- Группировка по `product_id + size_code + color_id`

**Склад ↔ Реализация:**
- Реализация использует данные склада (только остатки > 0)
- Валидация: нельзя реализовать больше, чем есть на складе

**Реализация ↔ Склад:**
- Реализация уменьшает остатки на складе
- Склад пересчитывается: `receipt_items - realization_items`

---

## 📊 Правила работы с данными

### Общие правила:

1. **Идентификация товара:**
   - Уникальная комбинация: `product_id + size_code + color_id`
   - Используется для группировки и расчета остатков

2. **Расчет остатков:**
   - Формула: `Math.max(0, receipt_qty - realization_qty)`
   - Отрицательные остатки невозможны
   - Нулевые остатки не отображаются на складе

3. **Валидация:**
   - Товар должен существовать перед поступлением
   - Остаток должен быть > 0 перед реализацией
   - Детские размеры: только 92-164 для категории 3

4. **Фильтрация:**
   - По категории: `category_id`
   - По бренду: `brand_id`
   - Поиск: артикул, название, бренд, цвет

5. **Сортировка размеров:**
   - Детские: 92 → 164 (числовая)
   - Взрослые: XS → XXXL (стандартный порядок)

---

## 🔧 API Endpoints

### Основные API:

**Товары:**
- `GET /api/products` - список товаров (фильтрация, поиск, пагинация)
- `POST /api/products/create` - создание товара

**Поступления:**
- `GET /api/receipts` - список поступлений
- `GET /api/receipts/[id]` - одно поступление
- `POST /api/receipts/create` - создание поступления

**Склад:**
- `GET /api/stock` - остатки на складе (для отображения)
- `GET /api/stock/stock-details` - детальные остатки (для формы реализации)

**Реализация:**
- `GET /api/realization` - список реализаций
- `GET /api/realization/[id]` - одна реализация
- `POST /api/realization/create` - создание реализации

**Справочники:**
- `GET /api/colors` - список цветов
- `GET /api/brands` - список брендов
- `GET /api/categories` - список категорий
- `GET /api/sizes/by-category?category_id=X` - размеры по категории

---

## 📝 Примечания

- Все API используют пагинацию для больших объемов данных
- Проверки на NULL значения добавлены в критичных местах
- Внешние ключи (`receipt_id`, `realization_id`) поддерживаются с fallback на time-based linking
- Детские размеры имеют специальную обработку для категории 3

---

**Версия:** 1.0  
**Дата:** 2024-12-19

