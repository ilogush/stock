# Отчет о противоречиях и несоответствиях в проекте

**Дата:** 2024-12-19

## 🔴 Критические противоречия

### 1. ❌ Противоречие в формате размера (size_code)

**Проблема:**
Размеры в БД хранятся в формате `"92 - 2 года"`, но при сохранении обрезаются до `"92"`.

**Где происходит:**
- `pages/api/receipts/create.ts:89` - обрезается до числовой части
- `pages/api/realization/create.ts:138, 178` - обрезается до числовой части
- `pages/api/orders/create.ts:23, 58` - обрезается до числовой части

**Где используется полный формат:**
- `pages/api/sizes/by-category.ts:31-32` - возвращает полный формат `"92 - 2 года"`
- `pages/api/stock/index.ts:286, 292` - извлекает числовую часть для сравнения

**Последствия:**
- Несоответствие между тем, что показывается пользователю и что сохраняется в БД
- Потенциальные проблемы при поиске и сопоставлении размеров
- Размеры могут не совпадать при сравнении

**Пример:**
```typescript
// В БД: "92 - 2 года"
// При сохранении: "92"
// При валидации: сравнивается "92" с "92 - 2 года"
```

---

### 2. ❌ Двойная валидация остатков в realization/create.ts

**Проблема:**
В `pages/api/realization/create.ts` остатки проверяются дважды:
1. Строки 52-105: ручная проверка через запрос к `receipt_items`
2. Строки 141-150: проверка через `validateStockForItems`

**Код:**
```typescript
// Первая проверка (строки 52-105)
for (const item of items) {
  // ... поиск в receipt_items
  if (stockItem.qty < item.qty) {
    return res.status(400).json({ error: 'Недостаточно товара' });
  }
}

// Вторая проверка (строки 141-150)
const stockValidation = await validateStockForItems(normalizedItems);
if (!stockValidation.valid) {
  return res.status(400).json({ error: 'Недостаточно товара' });
}
```

**Последствия:**
- Дублирование логики
- Разные алгоритмы проверки могут давать разные результаты
- Первая проверка не учитывает реализации (только поступления)
- Вторая проверка правильная (учитывает поступления - реализации)

**Рекомендация:**
Удалить первую проверку (строки 52-105), оставить только `validateStockForItems`.

---

### 3. ❌ Несоответствие в обработке color_id

**Проблема:**
Разные подходы к обработке `color_id` в разных местах:

**Вариант 1:** Проверка на `null`, `undefined`, `0`
```typescript
// pages/api/receipts/create.ts:63
if (item.color_id !== null && item.color_id !== undefined && item.color_id !== 0) {
  const colorId = parseInt(item.color_id);
  // ...
}
```

**Вариант 2:** Использование `|| null`
```typescript
// pages/api/receipts/create.ts:90
color_id: item.color_id || null
```

**Вариант 3:** Проверка через `parseInt`
```typescript
// pages/api/stock/index.ts:174
const colorId = row.color_id ? (parseInt(row.color_id) || row.color_id) : null;
```

**Вариант 4:** Прямое использование
```typescript
// pages/api/stock/stock-details.ts:78
const colorId = row.color_id || null;
```

**Последствия:**
- Несогласованность в обработке
- `color_id = 0` может интерпретироваться по-разному
- Потенциальные ошибки при валидации

**Рекомендация:**
Унифицировать обработку: `color_id` может быть `null` или положительным числом, `0` = `null`.

---

## ⚠️ Важные несоответствия

### 4. ⚠️ Дублирование валидации детских размеров

**Проблема:**
Список детских размеров дублируется в трех местах:

1. `pages/api/receipts/create.ts:74`
2. `pages/api/realization/create.ts:121`
3. `pages/api/stock/index.ts:276-277`

**Код:**
```typescript
const childrenSizes = ['92', '98', '104', '110', '116', '122', '134', '140', '146', '152', '158', '164'];
```

**Последствия:**
- При изменении списка нужно обновлять в трех местах
- Риск расхождений между валидациями

**Рекомендация:**
Вынести в константу в отдельный файл (например, `lib/constants.ts`).

---

### 5. ⚠️ Разные подходы к извлечению числовой части размера

**Проблема:**
В разных местах по-разному извлекается числовая часть:

**Вариант 1:** `split(' ')[0]`
```typescript
// pages/api/receipts/create.ts:76
const sizeNumber = item.size_code.split(' ')[0];
```

**Вариант 2:** `split(' ')[0].trim()`
```typescript
// pages/api/stock/index.ts:286
const sizeNumber = size.split(' ')[0].trim();
```

**Последствия:**
- Несогласованность
- Потенциальные проблемы с пробелами

**Рекомендация:**
Использовать единую функцию нормализации размера.

---

### 6. ⚠️ Несоответствие в расчете остатков

**Проблема:**
В `pages/api/realization/create.ts` (строки 52-105) остаток рассчитывается только из `receipt_items`, без учета `realization_items`:

```typescript
// Ищет только в receipt_items
const { data: matches } = await supabaseAdmin
  .from('receipt_items')
  .select('...')
  .eq('product_id', item.product_id)
  .eq('size_code', sizeVariant)
  .eq('color_id', item.color_id);

// Суммирует только поступления
const totalQty = matches.reduce((sum, match) => sum + (match.qty || 0), 0);
```

**Правильный расчет:**
```typescript
// Должно быть: receipt_items - realization_items
```

**Последствия:**
- Первая проверка может показать товар как доступный, хотя он уже реализован
- Вторая проверка (`validateStockForItems`) правильная, но первая может ввести в заблуждение

---

### 7. ⚠️ Разные форматы группировки данных

**Проблема:**
В `pages/api/stock/index.ts` и `pages/api/stock/stock-details.ts` используются разные форматы группировки:

**stock/index.ts:**
- Группировка: по артикулу → цвета → размеры
- Формат: `{ article, colors: { colorId: { sizes: {...} } } }`

**stock-details.ts:**
- Группировка: плоский список
- Формат: `[{ product_id, article, size_code, color_id, qty }]`

**Последствия:**
- Разные форматы для одной и той же информации
- Фронтенд должен обрабатывать оба формата

---

## 📝 Наблюдения

### 8. 📝 Множественные системы обработки ошибок

**Наблюдение:**
В проекте используются несколько систем обработки ошибок:

1. `lib/api/errorHandling.ts` - `handleDatabaseError`, `handleGenericError`
2. `lib/unified/errorHandler.ts` - `sendErrorResponse`, `sendValidationErrors`
3. `lib/errorService.ts` - `createApiError`, `handleDatabaseError`
4. `lib/errorHandler.ts` - `handleApiError`, `logError`

**Последствия:**
- Дублирование функционала
- Разные форматы ответов об ошибках
- Сложность поддержки

---

### 9. 📝 Дублирование логики расчета остатков

**Наблюдение:**
Логика расчета остатков дублируется в нескольких местах:

1. `pages/api/stock/index.ts` - расчет для отображения склада
2. `pages/api/stock/stock-details.ts` - расчет для формы реализации
3. `lib/stockService.ts` - унифицированная функция (но не везде используется)
4. `lib/warehouseChecker.ts` - еще одна версия
5. `lib/stockValidator.ts` - проверка остатков

**Последствия:**
- Дублирование кода
- Риск расхождений в логике
- Сложность поддержки

---

### 10. 📝 Несогласованность в нормализации размеров

**Наблюдение:**
В `pages/api/realization/create.ts` есть функция `normalizeSizeForSearch`, которая обрабатывает:
- Кириллицу/латиницу (М/M)
- Размеры с "/"
- Числовые размеры

Но эта функция используется только в одном месте, а в других местах используется просто `split(' ')[0]`.

**Последствия:**
- Несогласованность в обработке размеров
- Потенциальные проблемы с поиском

---

### 11. 📝 Разные подходы к валидации color_id

**Наблюдение:**
В разных местах разные проверки:

**receipts/create.ts:**
```typescript
if (item.color_id !== null && item.color_id !== undefined && item.color_id !== 0) {
  const colorId = parseInt(item.color_id);
  if (isNaN(colorId) || colorId <= 0) {
    // ошибка
  }
}
```

**realization/new.tsx:**
```typescript
if (!currentItem.color_id || currentItem.color_id === 0) {
  // ошибка
}
```

**Последствия:**
- Разные правила валидации
- Несогласованность в поведении

---

### 12. 📝 Несоответствие в использовании receipt_id/realization_id

**Наблюдение:**
Код поддерживает как внешние ключи (`receipt_id`, `realization_id`), так и time-based linking, но:
- В `receipts/create.ts` и `realization/create.ts` не устанавливается `receipt_id`/`realization_id` при создании
- Связь происходит только по времени

**Последствия:**
- Внешние ключи не заполняются при создании новых записей
- Нужно запускать скрипт `fill-foreign-keys.js` для заполнения

**Рекомендация:**
Устанавливать `receipt_id`/`realization_id` сразу при создании.

---

### 13. 📝 Разная обработка color_id в stock/index.ts и stock-details.ts

**Проблема:**
В `stock/index.ts` и `stock-details.ts` по-разному обрабатывается `color_id`:

**stock/index.ts:**
```typescript
const colorId = row.color_id ? (parseInt(row.color_id) || row.color_id) : null;
```

**stock-details.ts:**
```typescript
const colorId = row.color_id || null;
```

**Последствия:**
- Несогласованность в обработке
- В `stock/index.ts` делается лишний `parseInt` для уже числового значения

**Рекомендация:**
Унифицировать обработку: использовать `row.color_id || null` везде.

---

### 14. 📝 Разная обработка product_id в stock/index.ts

**Проблема:**
В `stock/index.ts` используется сложная логика получения `product_id`:

```typescript
const productId = row.product?.id || row.product_id;
```

В то время как в `stock-details.ts` используется просто:
```typescript
row.product_id
```

**Последствия:**
- Несогласованность
- В `stock/index.ts` делается join с `products`, что может быть избыточно

**Рекомендация:**
Использовать `row.product_id` везде, так как это поле есть в `receipt_items`.

---

## 🔧 Рекомендации по исправлению

### Приоритет 1 (Критично):

1. **Унифицировать формат size_code:**
   - Решить: хранить полный формат `"92 - 2 года"` или только `"92"`
   - Если полный - убрать обрезание при сохранении
   - Если только числовой - обновить API sizes/by-category

2. **Удалить двойную валидацию в realization/create.ts:**
   - Удалить первую проверку (строки 52-105)
   - Оставить только `validateStockForItems`

3. **Унифицировать обработку color_id:**
   - Создать функцию нормализации: `normalizeColorId(value) => number | null`
   - Использовать везде: `color_id = 0` → `null`, `color_id = null` → `null`

### Приоритет 2 (Важно):

4. **Вынести константы детских размеров:**
   - Создать `lib/constants.ts` с `CHILDREN_SIZES`
   - Использовать везде

5. **Создать функцию нормализации размера:**
   - `normalizeSizeCode(size: string): string` - извлекает числовую часть
   - Использовать везде вместо `split(' ')[0]`

6. **Исправить расчет остатков в realization/create.ts:**
   - Удалить первую проверку (она неправильная)
   - Использовать только `validateStockForItems`

7. **Устанавливать receipt_id/realization_id при создании:**
   - В `receipts/create.ts` устанавливать `receipt_id` при создании `receipt_items`
   - В `realization/create.ts` устанавливать `realization_id` при создании `realization_items`

### Приоритет 3 (Желательно):

8. **Унифицировать системы обработки ошибок:**
   - Выбрать одну систему (рекомендуется `lib/unified/errorHandler.ts`)
   - Мигрировать все API на единую систему

9. **Использовать stockService.ts везде:**
   - Заменить дублированную логику на вызовы `stockService.ts`
   - Удалить дублирующие функции

10. **Унифицировать форматы ответов API:**
    - Привести `stock/index.ts` и `stock-details.ts` к единому формату (или явно документировать различия)

---

## 📊 Статистика противоречий

- **Критических:** 3
- **Важных:** 4
- **Наблюдений:** 7
- **Всего:** 14

---

## ✅ Итог

Основные проблемы:
1. Формат размера не согласован между БД и кодом
2. Двойная валидация остатков с разной логикой
3. Несогласованная обработка `color_id`
4. Дублирование логики в разных местах

**Следующие шаги:**
1. Унифицировать формат `size_code`
2. Удалить двойную валидацию
3. Создать функции нормализации для `color_id` и `size_code`
4. Вынести константы в отдельный файл

